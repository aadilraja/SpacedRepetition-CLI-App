# SpacedRepetition-CLI-App
A lightweight command-line spaced-repetition tracker that reads topics from a Markdown file, tracks review intervals in SQLite, and shows which topics are due for revision.
